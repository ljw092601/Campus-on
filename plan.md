# 동아메이트(Dong-A Mate) — 외국인 유학생 학교 안내 앱 기획

> 대학교 외국인 유학생을 위한 캠퍼스 생활 안내 모바일 애플리케이션
> 작성일: 2026-07-10 · 최종 갱신: 2026-08-31 · 상태: **MVP 기능 구현 + 디자인 리뉴얼 진행 중**
> (구 명칭 "Campus-On" — 2026-08 앱명 **동아메이트** 확정, 저장소명은 Campus-on 유지)

### 확정된 방향 (2026-07-10)
- **플랫폼**: iOS + Android 둘 다 → **Flutter** 로 동시 개발
- **범위**: 특정 한 대학교 전용 (확장은 향후 과제)
- **MVP 핵심 기능**: ① 캠퍼스 지도·시설 안내 ② 행정 절차 가이드
- **지원 언어**: **한국어 + 영어** (추후 중국어 등 확장)
- **행정 가이드**: **틀(카테고리 구조)만 먼저** 구축, 세부 콘텐츠는 추후 보강
- **지도**: **카카오맵 SDK**
- **데이터**: 학교가 지원 의사 표명 ✅ (단, 공식 API 존재 여부는 확인 필요)
- **제약**: 1인 개발 · 기간 **1개월** · 예산 **최대 $500** → 범위를 얇고 견고하게 유지

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 앱 이름 | **동아메이트 / Dong-A Mate** (확정, 2026-08) — 신규 엠블럼 런처 아이콘·스플래시 적용 |
| 한 줄 소개 | 외국인 유학생이 입학부터 졸업까지 학교 생활에 필요한 정보를 한 곳에서 |
| 핵심 사용자 | 특정 대학교의 외국인 유학생 (교환학생 + 학위과정) |
| 해결하려는 문제 | 언어 장벽, 흩어진 정보, 행정 절차의 복잡함, 커뮤니티 부재 |
| 플랫폼 | **iOS + Android 둘 다** (확정) |
| 개발 방식 | **Flutter** (확정) |

---

## 2. 타깃 사용자 (페르소나)

- **교환학생 A** — 한 학기만 체류, 빠른 적응과 생활 정보(교통·식당·행사)가 중요
- **학위과정 유학생 B** — 장기 체류, 행정(비자·수강신청·성적)과 커뮤니티가 중요
- 공통 니즈: **영어 지원**, **오프라인 접근성**, **신뢰할 수 있는 최신 정보**

> ✅ 지원 언어: MVP는 **한국어 + 영어**. 이후 유학생 비중이 높은 언어(중국어 등)부터 순차 확장.

---

## 3. 핵심 기능 (MVP 후보)

### ⭐ MVP (1차 출시)
1. **캠퍼스 지도 & 시설 안내** — 건물, 강의실, 식당, 도서관, 편의시설 위치
2. **행정 절차 가이드** — **카테고리 틀 우선 구축**, 세부 콘텐츠는 추후 보강
3. **다국어 지원 (기반 기능)** — 한국어 + 영어로 제공 (유학생 앱의 전제)

> 💡 MVP는 "지도 + 행정 가이드 틀 + 한/영"에 집중해 빠르게 출시하고, 사용자 반응을 보며 확장합니다.
> 다국어는 별도 기능이라기보다 모든 화면의 기반이므로 처음부터 구조에 반영합니다.

#### 행정 절차 가이드 — 카테고리 틀 (초안)
각 항목은 지금은 "제목 + 자리표시자"만 두고, 실제 절차·서류·링크는 추후 국제교류처 자료로 채웁니다.

- **입국·체류**: 비자, 외국인등록증(ARC), 체류기간 연장
- **주거**: 기숙사 신청, 교외 원룸/하숙 구하기
- **생활 인프라**: 은행 계좌 개설, 휴대폰 개통, 교통카드
- **건강·보험**: 건강보험 가입, 교내 보건소, 병원 이용
- **학교 행정**: 수강신청, 성적/증명서 발급, 국제교류처 방문 안내
- **긴급/도움**: 긴급 연락처, 분실·사건사고 대응, 상담 창구

> 각 카테고리는 동일한 구조(개요 → 준비물 → 단계 → 관련 링크/위치)로 통일해 콘텐츠 보강이 쉬운 템플릿을 사용.

### 향후 확장 후보 (Phase 2+)
4. **공지사항 & 알림** — 학교/유학생회 공지 푸시 알림
5. **학사 정보** — 학사일정, 수강신청 안내, 성적/졸업요건 (링크 또는 연동)
6. **생활 정보** — 주변 맛집, 교통, 병원, 통신사, 긴급 연락처
7. **커뮤니티/Q&A** — 유학생 간 정보 공유, 멘토링
8. **일정/할 일 관리** — 개인 학사 일정, 마감일 리마인더

---

## 4. 정보 & 데이터 소스

학교 측이 **지원 의사를 표명**했으나 공식 API 존재 여부는 아직 미확인 → 우선 **자체 DB 기반**으로 설계하고, API 확인되면 연동하는 유연한 구조로 진행.

| 데이터 | 출처 | 연동 방식(안) |
|--------|------|----------------|
| 캠퍼스 지도 | 학교 제공 지도 + 지도 API | Google Maps/네이버 지도 위에 시설 좌표 오버레이 |
| 시설 정보 | 학교 제공 자료 | **자체 DB** (초기: 수동 정리) |
| 행정 절차 콘텐츠 | 국제교류처 자료 정리 | **자체 DB/CMS** (다국어 텍스트) |
| 학사일정/공지 (Phase 2) | 학교 포털 | ❓ 공식 API 확인 후 결정 (API/크롤링/수동) |
| 사용자 계정 | (MVP는 로그인 없이 열람 가능하게) | 향후 필요 시 이메일/소셜 |

> ✅ **액션 아이템**: 국제교류처에 ① 시설/지도 데이터 ② 행정 절차 최신 자료 ③ 공식 API·데이터 제공 가능 여부를 문의.

---

## 5. 기술 스택 (안)

- **프론트엔드**: **Flutter** (iOS/Android 동시, 확정)
- **백엔드/DB**: **Firebase** 추천 — Firestore로 시설·행정 콘텐츠 관리, 빠른 MVP에 적합 (자체 서버는 확장 시 검토)
- **지도**: **카카오맵 SDK** (Kakao Maps) — 국내 캠퍼스 건물명/실내 정보에 유리, 무료 쿼터 활용
- **다국어(i18n)**: Flutter `intl` + ARB 파일, 처음부터 영어·한국어 리소스 분리 관리
- **인증**: MVP는 비로그인 열람, Phase 2에서 이메일/소셜 도입
- **푸시 알림**: Firebase Cloud Messaging (Phase 2, 공지 기능과 함께)

---

## 6. 개발 로드맵 (1개월 · 1인 개발)

주 단위로 나눈 현실적인 일정입니다. 1인 개발이므로 각 주차는 순차 진행.

- **1주차 — 설계 & 기반**: 정보 구조(IA), 와이어프레임, 디자인 토큰, Flutter 프로젝트 셋업, Firebase·카카오맵 키 발급, i18n(ARB) 구조
- **2주차 — 캠퍼스 지도**: 카카오맵 연동, 시설 좌표/정보 데이터 입력, 시설 목록·상세·검색
- **3주차 — 행정 가이드 + 다국어**: 카테고리 틀 구현, 공통 상세 템플릿, 한/영 문자열 완성, 오프라인 캐싱
- **4주차 — QA & 출시 준비**: 버그 수정, 접근성·성능 점검, 스토어 메타데이터/스크린샷, 심사 준비

> ⚠️ 1개월 안에 끝내려면 **비로그인 열람 / 알림·커뮤니티 제외 / 행정 콘텐츠는 틀만**을 반드시 지켜야 함. 범위 추가는 출시 후로.

---

## 6-1. 예산 (최대 $500)

| 항목 | 예상 비용 | 비고 |
|------|-----------|------|
| Apple Developer Program | **$99/년** | iOS 출시 필수 |
| Google Play 개발자 등록 | **$25 (1회)** | Android 출시 필수 |
| Firebase | **$0** | Spark(무료) 요금제로 MVP 충분 |
| 카카오맵 API | **$0** | 무료 쿼터 내 사용 |
| 도메인/개인정보처리방침 호스팅 | ~$15/년 | 무료 대안(GitHub Pages) 가능 |
| 예비비 | 나머지 | 아이콘/폰트 등 소소한 에셋 |
| **합계(1차년)** | **약 $140 + α** | $500 내 충분, 여유 있음 |

> 대부분의 비용은 스토어 등록비. 개발 인프라는 무료 티어로 커버 가능해 예산 여유가 큼.

---

## 7. 남은 열린 질문 (다음 논의)

1. 초기 콘텐츠(시설·행정 정보)를 **누가 정리·입력**할지 — 개발자 본인이 초기 데이터 정리 예정이나, 국제교류처 자료 협조 시점 확인 필요
2. ~~앱 아이콘·이름 최종 확정~~ → ✅ **해결** (2026-08): **동아메이트** + 신규 엠블럼 아이콘·스플래시 적용
3. **강의실 호수 실데이터** — 사용자가 추후 제공 예정. 그때까지 층별 안내에서 파생한 placeholder 사용 (섹션 10 참조)
4. **학식 API 스펙** — 학교 측 제공 예정, 미확정. 그때까지 mock 예시 식단 사용 (섹션 11 참조)

> ✅ 해결됨: 플랫폼(iOS+Android), 개발방식(Flutter), MVP 기능, 확장 범위(단일 대학),
> 지원 언어(한/영), 행정 가이드 접근(틀 우선), 지도(카카오맵), 인력/기간/예산(1인·1개월·$500)

---

## 8. 다음 단계

- [x] **1주차 UX 설계** 완료 → `_workspace/01_ux_design.md`, QA 🟢 통과
- [x] **2주차 앱 골격 + 지도·시설 기능** 완료 → `_workspace/02_app_code/`, `02_app_architecture.md`, `03_api_integration.md`, QA 🟢 통과
  - Flutter 골격(Riverpod+go_router), 디자인 토큰(ThemeExtension), i18n(ARB 한/영), 카카오맵, 시설 목록/상세/검색(S1/S2/S3/S4/S8), Firestore+mock 데이터 계층
- [x] **빌드 검증** 완료 (2026-07-14): Flutter 3.44.6 stable로 `pub get`→gen-l10n→`analyze`(🟢 No issues)→`test`(🟢 통과). 실빌드가 잡은 결함(intl 버전 충돌·const 오류·lint 11) 전부 수정 → `05_qa_report.md` 라운드 2
- [x] **실기기 런타임 검증** 완료 (2026-07-14): Android 에뮬레이터(Pixel_7)에 실제 빌드·설치·실행. 홈(S1)·**지도(S2 카카오 지도 실렌더)**·i18n 화면 동작 스크린샷 확인. 실기기 결함(Android compileSdk 36, 매니페스트 INTERNET/cleartext, 카카오맵 서비스 활성화) 전부 해소 → `05_qa_report.md` 라운드 3
  - 카카오맵: **제품 설정→카카오맵→활성화 ON** 이 필수였음(키·도메인 아님). 실 캠퍼스 좌표·iOS 런타임·Firestore 실연동은 이후 과제
- [x] **3주차 — 가이드 + 설정/즐겨찾기 + 지도 마커 + 오프라인** 완료 (2026-07-14):
  - **가이드 S5~S7**: 카테고리 목록(6종·항목수 배지) → 항목 목록(published 우선 정렬·즐겨찾기 별) → 행정 상세(개요·준비물·단계·관련링크/위치 4섹션 아코디언, 메타 소요시간·난이도, 외부링크, 관련 위치 카드→지도 딥링크). 콘텐츠는 **틀 우선**(외국인등록증 발급 1건만 완성 예시, 나머지 17건 comingSoon)
  - **설정 S9**: 언어 전환 + 즐겨찾기 진입 + 정보(앱 소개·데이터 출처·문의하기·개인정보처리방침 외부링크·오픈소스 라이선스·버전)
  - **즐겨찾기 S10**: 시설/가이드 세그먼트, 스와이프 삭제 + 실행취소 스낵바, 빈 상태 CTA. SharedPreferences 로컬 저장
  - **지도 6색 커스텀 마커**: 카테고리별 핀 PNG 6종(`assets/markers/`) 생성 → `MarkerIcon.fromAsset`(base64, 오프라인) 적용. 실기기에서 6색 렌더 확인
  - **오프라인 캐싱 마감**: Firestore 영속성 명시 + 모든 읽기 경로(시설·가이드 getAll/getById/getByCategory) `Source.cache` 폴백
  - 검증: `analyze` 🟢 No issues · `test` 🟢 4건(가이드 플로우·즐겨찾기 지속·스모크) · Android 에뮬레이터 실기기 전 화면 스크린샷 확인 → `05_qa_report.md` 라운드 4
- [ ] 국제교류처에 데이터/API 제공 가능 여부 문의 (섹션 4 액션 아이템)
- [x] **오늘의 학식 페이지 구현** (2026-08-31, 커밋 1a90cd3) → 섹션 11 참조. 남은 것: 학교 식단 API 연동(스펙 대기)
- [ ] **학식 API 연동** (학교 측 스펙 대기 — `ApiDiningRepository` 추가 후 `diningRepositoryProvider` 교체, 섹션 11)
- [ ] **강의실 호수 실데이터 반영** (사용자 제공 대기 — `classroom_providers.dart` 교체, 섹션 10)
- [ ] **Firestore 시드 재업로드** (건물 영문명 48종 반영)
- [ ] 실 이름(호실 명칭) 영문화 — 2천여 건이라 보류, 호수 실데이터 도입과 함께 재검토
- [x] **카카오 실 키 연동 완료** (2026-07-16): JavaScript 키를 `env.json`(`--dart-define-from-file`)으로 주입 → Android 에뮬레이터에서 **실 카카오맵 타일·6색 커스텀 마커·동아대 승학캠퍼스 실좌표**가 렌더됨을 스크린샷으로 확인. 카카오 콘솔(카카오맵 활성화·Web 도메인)도 유효. `env.json`은 `.gitignore` 제외. RT-1(첫 진입 마커) 수정도 실 타일에서 재확인
  - ⚠️ **빌드 환경 이슈**: 프로젝트 경로에 한글(`추가-기능-추천`)이 있어 Gradle이 non-ASCII 경로를 거부 → 이번엔 ASCII 경로로 임시 복사해 빌드. 향후 워크트리/폴더명은 **영문으로 생성** 필요(또는 `android/gradle.properties`에 `android.overridePathCheck=true`)
- [x] **Firebase 실연동 완료 (2026-07-21)**: 프로젝트 `campus-f4748` 생성 → `flutterfire configure`(Android/iOS 앱 등록, `firebase_options.dart`·`google-services.json` 실값) → Firestore DB 생성(서울 `asia-northeast3`) → 보안 규칙 배포(공개 읽기 전용·쓰기 차단) → 시드 업로드(시설 6·가이드 18, 동아대 좌표) 및 서버 읽기 검증. 시드 JSON은 `mock_data.dart`에서 자동 생성(`tool/firestore_seed/export_seed_test.dart`) — 수동 편집 금지. 서비스 계정 키는 로컬 보관(gitignore). 앱 구동 시 `--dart-define=USE_FIRESTORE=true` 필요(기본값은 여전히 mock)
- [x] 앱 패키지명 확정·적용 (2026-07-21): `io.github.ljw092601.campuson` (Android applicationId/namespace + iOS 번들 ID, `com.example` 제거 — Play 등록 거부 사유 해소)
- [~] **4주차 (진행 중)** — QA 점검 완료(2026-07-15), 스토어 준비는 다음:
  - [x] **QA 점검**: 독립 코드 감사(접근성·i18n·성능·정합성·보안) + 실기기 재검증. **런타임 결함 2건 발견·수정** — RT-1 지도 첫 진입 마커 미표시(`onMapCreated` 명시 addMarker), RT-2 전화/이메일/외부링크 무동작(Android `<queries>`·iOS `LSApplicationQueriesSchemes`/ATS). 접근성 A-1/3/4/5/6·성능 P-1·정합 X-1/3 반영. `analyze`/`test` 🟢 → `05_qa_report.md` 라운드 5
  - [x] **실 캠퍼스 좌표**: 지도 중심·시설을 동아대 승학캠퍼스(부산)로 이동(상세 건물은 추후 실측)
  - [ ] **스토어 배포 준비** (iOS+Android, 앱명 **동아메이트**·동아대 명시): 메타데이터·스크린샷(신규 홈 디자인 기준)·개인정보처리방침·심사 자료
  - [ ] 상세 건물/행정 콘텐츠 입력, 오프라인 연결 배너(Phase-2), iOS 실기기 런타임(Mac 필요)
- [~] **건물 층별 안내 × 지도 핀** (2026-08-17 계획 수립, 진행 중) → 섹션 9 참조
  - [x] 좌표 수집 테스트: 공식 캠퍼스맵 페이지에서 48건 자동 추출 성공 → `tool/floor_guide_parser/building_coords.csv`
  - [x] **파서 + 시드 + 데이터 계층 절반** (2026-08-17, 9-3 ①·② 일부):
    - md 파서(`parse_floor_guide.py`) → `building_data.g.dart` 생성 (건물 48 · 층 249, 검증 통과)
    - `Facility`에 campus/buildingCode/hasFloorInfo 추가, `BuildingFloors` 엔티티 신설
    - mock 가짜 시설 6건 → 실 건물 48건으로 **교체 완료** (가이드 관련 위치 재매핑: 국제교류처→b04, 도서관→s10)
    - 시드 파이프라인 확장: `building_floors.seed.json` + seed.mjs `--prune` 옵션
    - **Firestore 실반영 완료**: facilities 48건 업로드 + 구 가짜 6건 삭제(prune), building_floors 34건 업로드, 보안 규칙(building_floors 공개 읽기) 배포
    - 검증: `analyze` 🟢 No issues · `flutter test` 🟢 4건 · 시드 내보내기 테스트(48/34/249 + 상호 참조 무결성) 🟢
  - [x] **9-3 ②③④ 구현 완료** (2026-08-17):
    - ② `FloorGuideRepository`(mock/Firestore, `Source.cache` 폴백) + `buildingFloorsProvider`(lazy 로드)
    - ③ 지도 캠퍼스 선택(SegmentedButton 승학·구덕·부민, 기본 승학) — 전환 시 마커 재필터 + `fitBounds` 카메라 이동. 캠퍼스 필터는 지도 전용(목록/검색은 전체). `/map?focus=` 딥링크가 다른 캠퍼스 건물이면 자동 캠퍼스 전환
    - ④ 핀 탭 → Peek 시트를 `DraggableScrollableSheet`로 확장(접힘 17%/펼침 75%, 스냅): 위로 드래그하면 층별 아코디언(층 배지 + 시설 수 → 펼치면 실 목록). 층 정보 없는 건물은 시트 고정. S4 시설 상세에도 동일한 층별 안내 섹션(`FloorAccordion` 공용 위젯)
    - 신규 l10n 6키(캠퍼스명·층별 안내·시설 수·오류), 신규 테스트 3건(데이터 불변식 48/34/249·캠퍼스 분포·아코디언 렌더/펼침)
    - 검증: `analyze` 🟢 No issues · `flutter test` 🟢 7건
  - [x] **실기기(에뮬레이터) 검증 완료** (2026-08-18, Pixel_7 · `--dart-define-from-file=env.json`):
    - 지도 탭: 승학캠퍼스 실 카카오 타일 위에 건물 핀들이 **실제 건물 위치와 정확히 일치** (공과대학1~5호관·대학본부·정문 등)
    - 핀 탭 → Peek 시트(이름·카테고리·View detail·Floor Guide 헤더) → 위로 드래그 → 층 아코디언(학생회관 B2F~6F, 층별 시설 수) → 1F 펼침 → 실 목록이 md 원본과 일치
    - 캠퍼스 전환: 구덕(핀 15개, 병원 구역 포함)·부민(핀 9개, 국제관·석당박물관·한국어학당 등) — fitBounds 카메라 이동 + 마커 재필터 + 열려있던 시트 자동 해제 정상
    - ⚠️ 이 체크아웃에 `google-services.json`이 없어 빌드 실패했었음 → `firebase apps:sdkconfig android <appId> --project campus-f4748 --out android/app/google-services.json`으로 복구 (gitignore 파일이라 체크아웃마다 필요할 수 있음)
    - `env.json`(카카오 JS 키) 사용자 입력 완료 — gitignore 확인됨
- [x] **앱명 확정 + 브랜딩** (2026-08 커밋 5527e91~1f16ed5): **동아메이트** + 신규 엠블럼 런처 아이콘(Android 어댑티브/iOS), 문구 포함 로고 스플래시(340dp·홀드 2.0초·페이드 0.5초)
- [x] **홈 화면 리디자인** (2026-08-31, 커밋 b1907f4 — design_template.png 기반, dev 에이전트 구현 + QA 에이전트 교차검증):
  - 네이비 히어로 타일(캠퍼스 사진 페이드 합성 + 도트 패턴) — **강의실 위치 검색 진입점**
  - 2×2 3D 일러스트 카드: 행정 가이드 → `/guide` · 시설 카테고리 → `/map/list` · 캠퍼스 지도 → `/map` · 오늘의 학식 → placeholder 페이지
  - 시설·행정 검색은 앱바 검색 아이콘으로 이동, 앱바에 투명 배경 엠블럼(28KB) + 앱명, KO|EN 토글 재스타일
  - **하단 탭 4→3개(홈/지도/설정)**: 가이드 탭 제거, `/guide` 라우트는 홈 브랜치로 이관(기존 딥링크 전부 유지), 내비바 플랫 스타일
  - QA 반영: 다크모드 아이콘 투명화, 검색 pill 대비 4.5:1, 배너 버튼 시맨틱, 번들 1.5MB 절감
- [x] **강의실 위치 검색 구현** (2026-08-31, 커밋 eaf82ce) → 섹션 10 참조
- [x] **건물 영문명 48종** (2026-08-31): `parse_floor_guide.py`의 `EN_NAMES`(id 기준) 매핑으로 생성·누락 시 생성 실패 검증 — EN 모드에서 앱 전체(지도·목록·검색·강의실 검색) 영문 건물명 표기. ⚠️ Firestore 모드는 **시드 재업로드 필요**(mock은 즉시 반영)

---

## 9. 신규 기능 — 건물 층별 안내 지도 연동 (2026-08-17)

`동아대학교_캠퍼스_건물층별안내.md`(3개 캠퍼스 · 건물 48개 · 층 249개)를 데이터 소스로,
**카카오맵에 건물 핀을 찍고 → 핀 탭 시 해당 건물의 층별 상세 정보를 보여주는** 기능.

### 9-1. 데이터 소스 현황

| 항목 | 내용 |
|------|------|
| 건물 수 | 승학 24 · 구덕 15 · 부민 9 = **48개** |
| 층 정보 | 34개 건물에 등록(249개 층), 나머지 14개는 "층별 정보 미등록" |
| 형식 | 건물별 `## 제목` + 층 테이블(`| 층 | 주요 시설 |`), 일부 건물은 소개 문구 포함 |
| ⚠️ 좌표 | **md에 위경도 없음** → 핀을 찍으려면 좌표 수집이 별도 작업으로 필요 (9-3 참조) |
| 언어 | 한국어 전용 → 층·실 명칭은 MVP에서 한국어 원문 유지, 건물명만 한/영 병기 (기존 `_pick` 폴백 활용) |

### 9-2. 설계 결정

#### ① 데이터 모델 — 층 정보는 별도 컬렉션(권장)

- `Facility`에 필드 추가: `campus`(`seunghak`/`gudeok`/`bumin`), `buildingCode`(S01·G03·B04 등), `hasFloorInfo`(bool)
- 층 상세는 **`building_floors/{facilityId}` 별도 컬렉션**으로 분리:
  ```
  { facilityId, floors: [ { floor: "B1F", rooms: ["구내서점", "보건진료소", ...] }, ... ] }
  ```
- 이유: 지도 화면은 전 시설 `getAll`을 하므로 층 텍스트(전체 수백 KB)를 facility 문서에 내장하면
  지도 첫 로딩·오프라인 캐시가 무거워짐. 층 정보는 **핀 탭 시 lazy load + `Source.cache` 폴백**(기존 패턴 재사용)
- mock/Firestore 이중 레포 구조 유지: `FloorGuideRepository` (mock + firestore 구현)

#### ② 파싱 파이프라인 — md → 시드 JSON 자동 생성

- `tool/floor_guide_parser/`에 파서 작성: md의 `## 건물명` + 층 테이블 → 시드 JSON
- 건물 코드(S01~S31, G01~G12, B01~B32)·캠퍼스(`# 승학캠퍼스` 헤더 기준)·소개 문구(이탤릭 줄) 추출
- 기존 원칙 유지: **시드 JSON 수동 편집 금지**, 파서 재실행으로만 갱신 (`export_seed_test.dart` 방식과 동일)
- "층별 정보 미등록" 건물도 시설로는 등록(핀은 찍되 `hasFloorInfo=false` → 층 탭 미노출)

#### ③ 좌표 수집 — ✅ 공식 캠퍼스맵 페이지에서 자동 추출 (2026-08-17 테스트 완료)

- 동아대 캠퍼스맵 페이지(md의 출처)가 `campusArr` JS 배열에 **건물명·위경도·캠퍼스구분(gubun)을 임베드**하고 있음을 확인
- `tool/floor_guide_parser/fetch_building_coords.py`: 페이지 다운로드 → 파싱 → **md 목차와 순서·이름 전수 대조 검증** → `building_coords.csv` 생성
- 테스트 결과: **48건 전부 추출 성공, 이름 대조 48/48 일치, 캠퍼스 클러스터 정상**
  (승학 24 lat 35.114~35.119 · 구덕 15 lat 35.119~35.122/lng 129.017~ · 부민 9 lat 35.103~35.106/lng 129.019~)
- 수동 수집 불필요해짐 — 3개 캠퍼스 전체를 처음부터 시드에 포함 가능. CSV는 스크립트로만 재생성(수동 편집 금지)

#### ④ UX — 핀 탭 → Peek 시트 확장(드래그 시트)

- 현행: 핀 탭 → `PeekSheet`(이름·카테고리·상세보기). 이를 **`DraggableScrollableSheet`로 확장**:
  - 접힘(기본): 현행 Peek과 동일 + "층별 안내" 유무 표시
  - 위로 드래그/탭: 층별 리스트 — 층 헤더(B3F→옥탑F 순) + 층별 시설 칩/텍스트, 아코디언 접기
- `FacilityDetailScreen`(S4)에도 동일한 층별 아코디언 섹션 추가(가이드 상세 S7의 4섹션 아코디언 패턴 재사용)
- 층·실 명칭은 한국어 원문 그대로 노출(영어 로케일에서도) — 안내 문구로 한계 명시

#### ⑤ 캠퍼스 전환

- 3개 캠퍼스가 서로 수 km 떨어져 있어 한 화면에 전부 담으면 핀이 뭉개짐
- 지도 상단(카테고리 필터 바 옆/위)에 **캠퍼스 선택 칩(승학·구덕·부민)** 추가, 기본 승학
- 전환 시 지도 중심 이동 + 해당 캠퍼스 시설만 마커 표시(`campus` 필드 필터)

### 9-3. 구현 단계 (제안 순서)

1. **파서 + 시드**: md 파서 작성 → 건물 48건 facility 시드 + 층 34건 floor 시드 생성. 좌표는 `building_coords.csv`(✅ 자동 추출 완료) 병합. 파서 단위 테스트(건물 수 48·층 수 249 검증). 기존 mock 시설 6건은 실 건물 시드로 교체
2. **데이터 계층**: `Facility` 필드 확장(campus/buildingCode/hasFloorInfo), `FloorGuide` 엔티티 + 레포(mock/Firestore) + provider, Firestore 시드 업로드·보안 규칙(읽기 전용) 확장
3. **지도**: 캠퍼스 선택 칩 + campus 필터, 건물 핀 렌더(기존 6색 마커의 building 색 재사용)
4. **층별 UI**: PeekSheet → 드래그 시트 확장(층 아코디언), FacilityDetail에 층 섹션 추가
5. **검증**: `analyze`/`test` + 에뮬레이터 실기기 — 핀 48개(승학 24) 렌더, 핀 탭→층 정보 표시, 오프라인 캐시 폴백 확인 → QA 리포트 라운드 추가

### 9-4. 열린 질문 / 리스크

1. ~~좌표 수집 공수~~ → ✅ **해결** (2026-08-17): 공식 캠퍼스맵 페이지에서 48건 자동 추출 성공 (9-2③)
2. ~~기존 mock 시설 6건과의 관계~~ → ✅ **확정** (2026-08-17): 실제 건물 시드로 **전부 교체**. mock은 테스트 픽스처로만 유지
3. **검색 연동(Phase 2)**: 실 이름(예: "보건진료소")으로 검색 → 해당 건물·층 안내로 이동. 249층 실 목록 전문 검색이라 인덱싱 방식 별도 검토
4. ~~영문화~~ → ✅ **확정** (2026-08-17): 명칭은 일단 **한국어로 구현**, 다른 언어는 추후 추가 (기존 `_pick` 폴백으로 영어 로케일에서도 한국어 노출)
5. **사이트 구조 변경 리스크**: 캠퍼스맵 페이지 구조가 바뀌면 좌표 스크립트가 깨질 수 있음 — 스크립트가 개수·이름 대조 검증에 실패하면 즉시 중단하도록 방어 처리됨

---

## 10. 신규 기능 — 강의실 위치 검색 (2026-08-31 구현 완료)

홈 히어로 타일에서 진입하는 강의실 검색. 커밋 `eaf82ce`.

### 10-1. 강의실 코드 체계 (확정)

- 형식: `캠퍼스문자+건물2자리`-`호수4자리` — 예) **S01-0301** = 승학 01번 건물 3층 01호
- 캠퍼스 문자: 승학=S · 구덕=G · 부민=B (기존 `Facility.buildingCode`와 일치)
- **호수 앞 2자리 = 층** (0301 → 3F). 지하(B1F)·옥탑의 번호 체계는 미정

### 10-2. 구현 내용

- **검색 UI** (`/classroom-search`, 루트 내비게이터 풀스크린): 건물 드롭다운 1개(48개, S→G→B 정렬, "S01 · 건물명" 표기) + 4자리 호수 입력. 호수는 **포함 일치** 필터(예: "101" → 0101·1010·1011…)로 제안 목록(층 배지 + 실 이름) 표시, 탭하면 자동 완성
- **검색 실행** → `/map?focus=<건물id>&floor=<앞2자리>` 딥링크: 지도 탭 전환 + 해당 건물 핀 포커스 + **층별 시트가 해당 층 펼쳐진 상태로 오픈** (`PeekSheet`/`FloorAccordion`에 `expandedFloor` 전달)
- **호수 placeholder 데이터**: 실데이터 제공 전까지 층별 안내에서 파생 — 3F에 실 N개면 0301..03NN을 실 이름과 짝지어 생성 (`classroom_providers.dart`, `TODO(room-data)`). 지하·옥탑은 제안에서 제외(직접 입력은 가능). **실데이터가 오면 이 프로바이더만 교체**
- 검증: `analyze` 🟢 · `test` 🟢 18건(호수 파생 단위 테스트 2건 신규) · 에뮬레이터에서 선택→필터→검색→지도+층 시트 실기 확인 (KO/EN 모두)

### 10-3. 남은 것

- [ ] 호수 실데이터 반영 (사용자 제공 대기)
- [ ] 층이 많은 건물에서 시트가 해당 층으로 **자동 스크롤** (현재는 펼쳐지되 스크롤 필요할 수 있음)
- [ ] 검색 화면 고도화(최근 검색 등) — 필요 시

---

## 11. 신규 기능 — 오늘의 학식 (2026-08-31 구현 완료, API 연동 대기)

홈 카드에서 진입하는 일별 학식 페이지 (`/home/dining`, 탭바 유지). 커밋 `1a90cd3`.

### 11-1. 데이터 전략 — 학교 API 예정, 스펙 미정

- 식단 데이터는 **학교 측 API로 수신 예정이나 스펙 미확정** (2026-08-31 기준)
- `DiningRepository` 인터페이스(`getMenus(date)`) 뒤에 격리 — 화면·프로바이더는 인터페이스만 의존.
  **스펙이 오면 `ApiDiningRepository` 구현체 추가 후 `repository_providers.dart`의
  `diningRepositoryProvider` 한 줄 교체** (`TODO(dining-api)`, 기존 mock↔Firestore 스왑 패턴과 동일)
- 그때까지 `MockDiningRepository`: 식당 3곳(승학·구덕·부민 학생식당, 임시), 날짜 기반 결정적 메뉴 순환,
  주말 휴무. 식당명·영업시간·가격 전부 임시값 — API 데이터로 통째 교체 대상
- `CafeteriaMenu`/`Meal` 엔티티에 `fromJson`/`toJson` 구비(API 응답 매핑 대비), 한/영 이름·영업시간 폴백 지원

### 11-2. UI

- 날짜 이동(◀ 오늘 ▶, 로케일별 날짜 포맷) + **"예시 식단" 안내 배너**(API 연동 전임을 명시)
- 식당별 카드: 캠퍼스 배지 + 식당명 + 영업시간, 조식/중식/석식 배지별 메뉴·가격,
  **지도에서 보기** → 해당 건물 핀 딥링크(`/map?focus=`), 휴무일은 "오늘은 운영하지 않아요."
- 검증: `analyze` 🟢 · `test` 🟢 21건(mock 레포 단위 테스트 3건 신규 — 평일 구성/주말 휴무/날짜 순환) · 에뮬레이터 KO 실기 확인(평일·주말)

### 11-3. 남은 것

- [ ] 학교 식단 API 스펙 확보 → `ApiDiningRepository` 연동 (오프라인 캐싱 정책도 그때 결정)
- [ ] 실제 식당 목록·영업시간·가격 반영 (API 또는 학교 자료)
